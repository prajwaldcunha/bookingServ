{
  "title": "Booking resource",
  "description": "Booking resource object",
  "type": "object",
  "properties": {
    "id": {
      "description": "Booking id",
      "type": "integer"
    },
    "first_name": {
      "description": "First name",
      "type": "string",
      "minLength": 1,
      "maxLength": 255
    },
    "last_name": {
      "description": "Last name",
      "type": "string",
      "minLength": 1,
      "maxLength": 255
    },
    "date_of_birth": {
      "description": "date of birth",
      "type": "string",
      "format": "date"
    },
    "checkin_datetime": {
      "description": "check-in date and time",
      "type": "string",
      "format": "date-time"
    },
    "checkout_datetime": {
      "description": "check-out date and time",
      "type": "string",
      "format": "date-time"
    },
    "totalprice": {
      "description": "total price of booking",
      "type": "string"
    },
    "deposit": {
      "description": "Deposit paid for booking",
      "type": "string"
    }
  },
  "required": [
    "first_name",
    "last_name",
    "date_of_birth",
    "checkin_datetime",
    "checkout_datetime",
    "totalprice",
    "deposit"
  ]
}